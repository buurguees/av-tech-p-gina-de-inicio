# NEXO AV - Arquitectura del Sistema Responsive

## 🏗️ Diagrama de Flujo

```
┌─────────────────────────────────────────────────────────────────┐
│                          Usuario accede                          │
│                       https://app.com/nexo-av                    │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  Login Page   │
                    │  (Compartida) │
                    └───────┬───────┘
                            │
                            │ Autenticación exitosa
                            ▼
                ┌───────────────────────────┐
                │   ResponsiveLayout.tsx    │
                │  (Detecta dispositivo)    │
                └───────────┬───────────────┘
                            │
            ┌───────────────┴───────────────┐
            │                               │
            ▼                               ▼
    ┌──────────────┐              ┌──────────────┐
    │  width ≥     │              │  width <     │
    │  1024px      │              │  1024px      │
    └──────┬───────┘              └──────┬───────┘
           │                             │
           ▼                             ▼
┌──────────────────────┐      ┌──────────────────────┐
│  NexoAvLayout.tsx    │      │ NexoAvLayoutMobile   │
│  (Desktop)           │      │ .tsx (Mobile)        │
├──────────────────────┤      ├──────────────────────┤
│ • Header fijo        │      │ • Header compacto    │
│   (superior)         │      │   (con safe areas)   │
│ • Sidebar fijo       │      │ • Bottom Navigation  │
│   (izquierda)        │      │   (inferior fija)    │
│ • Área de contenido  │      │ • Pantalla completa  │
│ • Timeout: 60 min    │      │ • Timeout: 30 min    │
└──────────┬───────────┘      └──────────┬───────────┘
           │                             │
           └──────────────┬──────────────┘
                          │
                          ▼
              ┌───────────────────────┐
              │  Rutas Compartidas    │
              │  (/dashboard, etc)    │
              └───────────┬───────────┘
                          │
              ┌───────────┴────────────┐
              │                        │
              ▼                        ▼
    ┌─────────────────┐    ┌─────────────────┐
    │  Desktop Pages  │    │  Mobile Pages   │
    │  (38 páginas)   │    │  (Optimizadas)  │
    └─────────────────┘    └─────────────────┘
```

## 🔄 Detección y Cambio de Layout

### Proceso de Detección

```typescript
// 1. Hook de detección (useDeviceDetection.ts)
┌─────────────────────────────────────┐
│ window.innerWidth                   │
│   ↓                                 │
│ < 768px    → isMobile = true        │
│ 550-1023px → isTablet = true        │
│ ≥ 1024px   → isDesktop = true       │
└─────────────────────────────────────┘

// 2. Decisión de layout (ResponsiveLayout.tsx)
┌─────────────────────────────────────┐
│ const isMobile = useIsMobile()      │
│   ↓                                 │
│ isMobile ? MobileLayout             │
│          : DesktopLayout            │
└─────────────────────────────────────┘

// 3. Eventos monitoreados
┌─────────────────────────────────────┐
│ • window.resize                     │
│ • window.orientationchange          │
│ • Actualización automática          │
└─────────────────────────────────────┘
```

## 📊 Estructura de Componentes

### Desktop Layout Structure

```
NexoAvLayout
├── Header (z-50, fixed top)
│   ├── Logo + Título
│   ├── Info Usuario
│   └── UserAvatarDropdown
│
├── Sidebar (z-40, fixed left)
│   ├── Dashboard (Home)
│   ├── Carpetas Colapsables
│   │   ├── Mapas
│   │   ├── Ventas
│   │   └── Compras
│   ├── Módulos Individuales
│   │   ├── Clientes
│   │   ├── Técnicos
│   │   ├── Proveedores
│   │   ├── Catálogo
│   │   ├── Calculadora
│   │   └── Informes
│   └── Sección Admin (fija inferior)
│       ├── Contabilidad
│       ├── Configuración
│       └── Auditoría
│
└── Main Content Area
    └── <Outlet /> (React Router)
        └── Páginas dinámicas
```

### Mobile Layout Structure

```
NexoAvLayoutMobile
├── Header (z-50, fixed top)
│   ├── Safe Area Insets
│   ├── Logo compacto
│   └── UserAvatarDropdown
│
├── Main Content (full screen)
│   ├── Safe Area Padding (top/bottom)
│   └── <Outlet /> (React Router)
│       └── Páginas móviles optimizadas
│
└── Bottom Navigation (z-40, fixed bottom)
    ├── Safe Area Insets
    └── 4-5 ítems principales
        ├── Dashboard/Maps
        ├── Clientes
        ├── Proyectos
        ├── Catálogo
        └── Más (Menú)
```

## 🎯 Flujo de Autenticación y Seguridad

```
┌─────────────────────────────────────────────────────────────────┐
│                    Usuario sin autenticar                        │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  /nexo-av     │ → Login Page
                    └───────┬───────┘
                            │
                            │ Credenciales
                            ▼
                ┌───────────────────────────┐
                │  Supabase Auth            │
                │  • Verificar credenciales │
                │  • Crear sesión           │
                │  • Obtener user info      │
                └───────────┬───────────────┘
                            │
                            ▼
                ┌───────────────────────────┐
                │  ResponsiveLayout         │
                │  • Detectar dispositivo   │
                │  • Cargar layout          │
                └───────────┬───────────────┘
                            │
                ┌───────────┴───────────┐
                │                       │
                ▼                       ▼
        ┌───────────────┐     ┌───────────────┐
        │ Desktop        │     │ Mobile        │
        │ checkAuth()    │     │ checkAuth()   │
        ├───────────────┤     ├───────────────┤
        │ 1. Sesión OK? │     │ 1. Sesión OK? │
        │ 2. User info  │     │ 2. User info  │
        │ 3. userId =   │     │ 3. userId =   │
        │    URL param? │     │    URL param? │
        │ 4. Permisos   │     │ 4. Permisos   │
        │ 5. Tema       │     │ 5. Tema       │
        └───────┬───────┘     └───────┬───────┘
                │                     │
                │ ✅ Todo OK          │ ✅ Todo OK
                ▼                     ▼
        ┌───────────────┐     ┌───────────────┐
        │ Redirección   │     │ Redirección   │
        │ inicial:      │     │ inicial:      │
        │ → /dashboard  │     │ → /lead-map   │
        │               │     │   (o según    │
        │               │     │    rol)       │
        └───────────────┘     └───────────────┘
```

## 🔐 Seguridad Multicapa

```
┌─────────────────────────────────────────────────────────────────┐
│                     CAPA 1: Supabase Auth                        │
│  • JWT Token validation                                          │
│  • Session management                                            │
│  • Auto-refresh tokens                                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  CAPA 2: Layout Auth Check                       │
│  • useEffect(() => checkAuth())                                  │
│  • Verificar sesión activa                                       │
│  • Obtener información del usuario                               │
│  • Validar userId en URL                                         │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  CAPA 3: Permisos por Rol                        │
│  • isAdmin, isManager, isSales, isTech                           │
│  • Módulos disponibles según rol                                 │
│  • Navegación filtrada                                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│              CAPA 4: Inactividad Timeout                         │
│  • Desktop: 60 minutos                                           │
│  • Mobile: 30 minutos                                            │
│  • Warning: 5 minutos antes                                      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                  CAPA 5: Auth State Listener                     │
│  • supabase.auth.onAuthStateChange()                             │
│  • SIGNED_OUT → redirect to /nexo-av                             │
│  • Session expired → auto logout                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 📦 Lazy Loading y Code Splitting

```
App.tsx (Bundle principal)
│
├── ResponsiveLayout (lazy)
│   ├── → Desktop Bundle
│   │   ├── NexoAvLayout
│   │   ├── Sidebar
│   │   ├── Header
│   │   └── Desktop Components
│   │
│   └── → Mobile Bundle
│       ├── NexoAvLayoutMobile
│       ├── Bottom Navigation
│       └── Mobile Components
│
└── Pages (todas lazy loaded)
    ├── Dashboard (lazy)
    ├── ClientsPage (lazy)
    ├── ProjectsPage (lazy)
    └── ... (todas las páginas)
```

### Beneficios:

1. **Bundle Size**: Solo se carga el código necesario
2. **Initial Load**: Más rápido al cargar solo lo esencial
3. **Memory**: Menor uso de memoria
4. **Network**: Menos datos transferidos

## 🎨 Sistema de Temas

```
┌─────────────────────────────────────────────────────────────────┐
│                    useNexoAvTheme Hook                           │
└───────────────────────────┬─────────────────────────────────────┘
                            │
            ┌───────────────┴───────────────┐
            │                               │
            ▼                               ▼
    ┌───────────────┐              ┌───────────────┐
    │ theme='light' │              │ theme='dark'  │
    └───────┬───────┘              └───────┬───────┘
            │                              │
            ▼                              ▼
┌──────────────────────┐      ┌──────────────────────┐
│ body.classList.add   │      │ body.classList.add   │
│ ('nexo-av-theme')    │      │ ('nexo-av-theme-dark')│
└──────────┬───────────┘      └──────────┬───────────┘
           │                             │
           └──────────────┬──────────────┘
                          │
                          ▼
              ┌───────────────────────┐
              │   global.css          │
              │   Variables CSS       │
              │   • --background      │
              │   • --foreground      │
              │   • --primary         │
              │   • --border          │
              │   • etc...            │
              └───────────────────────┘
```

## 🚀 Optimizaciones de Performance

### 1. Code Splitting
- ✅ Layouts lazy loaded
- ✅ Páginas lazy loaded
- ✅ Componentes pesados lazy loaded

### 2. Bundle Optimization
- ✅ Tree shaking habilitado
- ✅ Solo código activo en bundle
- ✅ Imports optimizados

### 3. Caching Strategy
- ✅ React Query para datos
- ✅ Supabase client singleton
- ✅ Memoización de componentes costosos

### 4. Responsive Loading
- ✅ Solo carga layout activo
- ✅ Lazy routes
- ✅ Suspense boundaries

## 📱 Estrategia Mobile-First

```
Breakpoints:
  Mobile:  0px    - 767px   (Smartphones)
  Tablet:  768px  - 1023px  (Tablets)
  Desktop: 1024px - ∞       (Laptops, Desktops)

CSS Strategy:
  Base:    Mobile styles (mobile-first)
  @media (min-width: 768px):  Tablet overrides
  @media (min-width: 1024px): Desktop overrides

Component Strategy:
  if (isMobile || isTablet) → Mobile Layout
  if (isDesktop)            → Desktop Layout
```

## 🔄 Estado y Sincronización

```
┌─────────────────────────────────────────────────────────────────┐
│                         Supabase Backend                         │
│  • PostgreSQL Database                                           │
│  • Real-time subscriptions                                       │
│  • Row Level Security (RLS)                                      │
└───────────────────────────┬─────────────────────────────────────┘
                            │
            ┌───────────────┴───────────────┐
            │                               │
            ▼                               ▼
    ┌───────────────┐              ┌───────────────┐
    │ Desktop State │              │ Mobile State  │
    └───────┬───────┘              └───────┬───────┘
            │                              │
            │   Mismo backend              │
            │   Mismas tablas              │
            │   Mismos permisos            │
            │                              │
            └──────────────┬───────────────┘
                           │
                           ▼
                   ┌───────────────┐
                   │ Sincronizado  │
                   │ en tiempo real│
                   └───────────────┘
```

---

**Mantenido por**: Equipo de Desarrollo AV TECH  
**Última actualización**: Enero 2026
