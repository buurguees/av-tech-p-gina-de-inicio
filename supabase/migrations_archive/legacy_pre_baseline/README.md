# Legacy Pre-Baseline Archive

This folder is reserved for the pre-baseline migration timeline.

## Purpose

- preserve the old migration history as audit evidence
- keep `supabase/migrations/` focused on the canonical baseline and future incremental migrations

## Move rule

Move legacy files here only after the baseline migration file is generated and reviewed.

Expected baseline naming:

- `<timestamp>_baseline_remote_20260303.sql`

Current status:

- folder prepared
- legacy timeline move pending baseline capture
